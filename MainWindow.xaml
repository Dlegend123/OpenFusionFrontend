<Window x:Class="fflauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fflauncher"
        mc:Ignorable="d"
              WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Title="OpenFusion Launcher"
        Height="620"
        Width="900">
    <Window.Resources>
        <Style x:Key="CarouselItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,18,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root"
                                CornerRadius="18"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.Effect>
                                <DropShadowEffect Color="#001822"
                                                  BlurRadius="18"
                                                  ShadowDepth="0"
                                                  Opacity="0.18"/>
                            </Border.Effect>
                            <ContentPresenter/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.01" ScaleY="1.01"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource CardColor}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                <Setter TargetName="Root" Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder"
            CornerRadius="16"
            Background="{DynamicResource BgColor}"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="1"
            SnapsToDevicePixels="True">

        <Grid>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <!-- Title bar -->
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0"
        Background="{DynamicResource BgColor}"
                        MouseDown="Window_MouseDown"
                        Panel.ZIndex="1"
                        CornerRadius="16,16,0,0"
                        SnapsToDevicePixels="True">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="OpenFusion Launcher"
                                   VerticalAlignment="Center"
                                   Margin="14,0,0,0"
                                   Foreground="{DynamicResource FgColor}"
                                   FontWeight="Bold"/>

                        <Button Grid.Column="1"
                                Content="—"
                                Width="40"
                                Height="32"
                                Margin="0"
                                FontFamily="Segoe UI Symbol"
                                FontSize="16"
                                Foreground="{DynamicResource FgColor}"
                                Click="Minimize_Click"
                                Style="{StaticResource TitleBarButtonStyle}"/>

                        <Button Grid.Column="2"
                                Content="□"
                                Width="40"
                                Height="32"
                                Margin="0"
                                Name="MaximizeButton"
                                FontFamily="Segoe UI Symbol"
                                FontSize="16"
                                Foreground="{DynamicResource FgColor}"
                                Click="Maximize_Click"
                                Style="{StaticResource TitleBarButtonStyle}"/>

                        <Button Grid.Column="3"
                                Content="X"
                                Width="40"
                                Height="32"
                                Margin="0"
                                FontFamily="Segoe UI Symbol"
                                FontSize="16"
                                Foreground="{DynamicResource FgColor}"
                                Click="Close_Click"
                                Style="{StaticResource TitleBarCloseButtonStyle}"/>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Background="{DynamicResource CardColor}" CornerRadius="12" Margin="10" Padding="16" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="Choose Server" FontSize="18" FontWeight="Bold" Margin="0,0,0,6"/>
                            <TextBlock Text="Swipe through your available launch targets." FontSize="13" Opacity="0.75"/>
                        </StackPanel>

                        <Button x:Name="SettingsButton" Content="Settings" Grid.Row="0" Grid.Column="1" VerticalAlignment="Top" Width="110" Height="40" Click="SettingsButton_Click"/>
                        <ListBox x:Name="ServerCarousel"
         PreviewMouseLeftButtonDown="Carousel_MouseDown"
         PreviewMouseMove="Carousel_MouseMove"
         PreviewMouseLeftButtonUp="Carousel_MouseUp"
         MouseLeftButtonUp="ServerCarousel_MouseLeftButtonUp"
                                           Grid.Row="1"
                                 SelectionChanged="ServerCarousel_SelectionChanged"
 Grid.ColumnSpan="2"
 Margin="0,16,0,0"
         Background="Transparent"
         BorderThickness="0"
         ScrollViewer.HorizontalScrollBarVisibility="Hidden"
ScrollViewer.VerticalScrollBarVisibility="Disabled"
         SelectionMode="Single"
IsSynchronizedWithCurrentItem="True"
         ItemContainerStyle="{StaticResource CarouselItemStyle}">
                            <ListBox.ItemsPanel>
                                    
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>

                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="280"
                Height="180"
                CornerRadius="18"
                ClipToBounds="True">

                                            <Grid>

                                                <!-- NORMAL CONFIG -->
                                                <Grid x:Name="NormalCard">

                                                    <Grid.Clip>
                                                        <RectangleGeometry RadiusX="18"
                                           RadiusY="18"
                                           Rect="0,0,280,180"/>
                                                    </Grid.Clip>

                                                <Image Stretch="UniformToFill"
       Opacity="0.75">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="Source" Value="{Binding ImagePath}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsAddNew}" Value="True">
                                                                    <Setter Property="Source" Value="{x:Null}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>

                                                <Border Background="#66000000"/>

                                                    <StackPanel Margin="14"
                                VerticalAlignment="Bottom">

                                                        <TextBlock Text="{Binding Name}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource FgColor}"/>

                                                        <TextBlock Text="{Binding Address}"
                                   FontSize="12"
                                   Foreground="{DynamicResource AccentColor}"
                                   Opacity="0.95"/>
                                                    </StackPanel>
                                                </Grid>

                                                <!-- ADD NEW TILE -->
                                                <Border x:Name="AddTile"
                        Background="{DynamicResource CardColor}"
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="18"
                        Visibility="Collapsed">

                                                    <Button Click="AddTile_Click"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Cursor="Hand"
                                                            Padding="0"
                                                            HorizontalAlignment="Stretch"
                                                            VerticalAlignment="Stretch">
                                                        <TextBlock Text="+"
                                                                   FontSize="42"
                                                                   FontWeight="Bold"
                                                                   Foreground="{DynamicResource AccentColor}"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center"/>
                                                    </Button>
                                                </Border>

                                            </Grid>
                                        </Border>

                                        <!-- SWITCH TEMPLATE -->
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsAddNew}" Value="True">
                                                <Setter TargetName="NormalCard" Property="Visibility" Value="Collapsed"/>
                                                <Setter TargetName="AddTile" Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>

                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="2"
              Margin="10"
              x:Name="ConfigScrollViewer"
              HorizontalScrollBarVisibility="Disabled"
              VerticalScrollBarVisibility="Hidden"
              PanningMode="VerticalOnly"
              CanContentScroll="False"
              PreviewMouseLeftButtonDown="ConfigDrag_MouseDown"
              PreviewMouseMove="ConfigDrag_MouseMove"
              PreviewMouseLeftButtonUp="ConfigDrag_MouseUp">

                    <StackPanel x:Name="ConfigDetailsPanel"/>
                </ScrollViewer>

                <Button x:Name="LaunchButton" Grid.Row="3" Content="Launch Game" Height="46" Margin="10" HorizontalAlignment="Stretch" Click="LaunchButton_Click"/>
            </Grid>
        </Grid>
    </Border>
        
</Window>
